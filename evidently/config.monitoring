evidently:
  monitoring:
    reference_dataset: "path/to/reference.csv"
    current_dataset: "path/to/current.csv"
    metrics:
      - data_drift
      - target_drift
      - performance
    output_directory: "/reports"
    interval: "daily"  # or "hourly", "weekly"
